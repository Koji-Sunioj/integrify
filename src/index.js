import React from 'react';
import ReactDOM from 'react-dom';
import Homepage from './App';

//based DOM rendering syntax to HTML element

ReactDOM.render(<Homepage />, document.getElementById('root'));

